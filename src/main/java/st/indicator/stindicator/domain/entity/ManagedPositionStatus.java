package st.indicator.stindicator.domain.entity;

public enum ManagedPositionStatus {
    EXTERNAL_UNMANAGED,
    NEEDS_CONFIGURATION,
    ACTIVE_MANAGED,
    ACTIVE,
    CLOSING,
    CLOSED,
    FAILED
}
